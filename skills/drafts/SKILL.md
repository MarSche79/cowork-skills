---
name: "drafts"
description: "Manage pending Teams reply drafts created by the Question Watcher automation. Lists pending drafts for a colleague and the manager, lets the user approve/edit/skip them, and sends approved replies via m365_send_chat_message. Trigger phrases: '/drafts', 'show drafts', 'show pending drafts', 'pending drafts', 'send draft N', 'send N', 'edit draft N ...', 'edit N ...', 'skip draft N', 'skip N', 'reply to a colleague', 'reply to the manager'. Also activates when the user replies in Teams self-chat with patterns like 'send 5',"
---

# /drafts — Teams Reply Draft Manager

You manage pending Teams reply drafts that the **Question Watcher** automation creates when a colleague Ding or your manager sends the user a question in their 1:1 Teams chat.

## State files

- `C:\Users\<username>\.copilot\question-watcher\watched.json` — chat config (chatIds, tone, email)
- `C:\Users\<username>\.copilot\question-watcher\drafts.json` — pending drafts (read & write)
- `C:\Users\<username>\.copilot\question-watcher\state.json` — last-seen watermarks (do not modify here)

## Draft record shape

Each entry in `drafts.json.drafts[]`:
```
{
  "id": 5,
  "chatRef": "a colleague" | "the manager",
  "chatId": "19:...@unq.gbl.spaces",
  "personName": "a colleague Ding",
  "incomingMessageId": "1779...",
  "incomingText": "stripped plain-text version of her question",
  "incomingCreatedDateTime": "2026-05-18T...Z",
  "draftText": "html body the user proposes the user sends",
  "draftPlainText": "plain-text version (for preview)",
  "contextSummary": "brief notes used to ground the reply",
  "status": "pending" | "sent" | "skipped" | "edited",
  "createdAt": "2026-05-18T...Z",
  "sentAt": null,
  "sentMessageId": null
}
```

## Commands

### List pending drafts
Triggered by: `/drafts`, `show drafts`, `pending drafts`, `what's pending`, `show me my drafts`.

Read `drafts.json`. Show ONLY entries with `status: "pending"`.

For each pending draft, render a numbered card with:
- 🆔 `#<id>` · **<personName>** · _<relative time, e.g. "12 min ago">_
- ❓ **Their question:** `incomingText` (truncate to ~300 chars with `…`)
- 📝 **the user's draft:**
  > `draftPlainText` (full text in blockquote)

Then list available actions:
> Reply with: `send <id>` · `edit <id> <new text>` · `skip <id>`

If `drafts[]` is empty or no pending entries, say:
> 📭 No pending drafts right now. The Question Watcher checks every 15 min during work hours.

### Send a draft
Triggered by: `send <id>`, `send draft <id>`, `approve <id>`, `ship <id>`, `go <id>`.

1. Read `drafts.json`, find the draft. If not found or already non-pending → friendly error.
2. Send via `m365_send_chat_message` with `contentType: html` and the draft's `draftText`. The `draftText` already includes the the user signature line at the bottom (see "Draft format" below).
3. On success: update the draft → `status: "sent"`, `sentAt: <now ISO>`, `sentMessageId: <response.id>`. Save `drafts.json`.
4. Confirm to the user: `✅ Sent draft #<id> to <personName>.` Show the sent plain-text below.

### Edit & send
Triggered by: `edit <id> <new text>`, `edit draft <id> <new text>`, `replace <id> with <new text>`.

1. Read `drafts.json`, find the draft.
2. Wrap the new text in the standard reply HTML envelope (paragraphs + the user signature — see template below). Preserve any explicit HTML formatting the user wrote.
3. Send via `m365_send_chat_message` with `contentType: html`.
4. Update the draft → `status: "edited"`, `draftText` (the new HTML), `draftPlainText` (the user's input), `sentAt`, `sentMessageId`. Save.
5. Confirm: `✅ Sent your edited reply to <personName>.`

### Skip / dismiss
Triggered by: `skip <id>`, `dismiss <id>`, `ignore <id>`, `delete draft <id>`.

Update the draft → `status: "skipped"`. Save. Confirm: `🗑️ Skipped draft #<id>.`

### Preview a specific draft
Triggered by: `show draft <id>`, `preview <id>`, `what's draft <id>`.

Show that single draft in the card format from "List pending drafts".

## Draft format (HTML envelope used for new edits)

When sending an *edited* reply, wrap user input as:
```html
<p>{paragraph 1}</p>
<p>{paragraph 2}</p>
...
<hr>
<p><strong style="color:#b8410e;">the user 🦞</strong><br><em>the user' Personal AI Assistant</em><br><span style="color:#777;font-size:11px;">This message was generated by the user on the user' behalf.</span></p>
```
Split on blank lines for paragraphs. Keep simple — no fancy styling unless user added it.

## Important rules

- **Never send a draft without an explicit `send N` / `edit N …` from the user.** Just listing or previewing does NOT send.
- **Always save `drafts.json` atomically:** read → modify → write the whole file. Preserve `nextId` and other entries.
- **Relay-friendly:** if the user's message looks like a bare command (`send 5`, `skip 3`, `edit 7 thanks I'll loop back tomorrow`), execute it without asking for confirmation — he's likely on mobile.
- **Multi-action**: `send 2,4` or `skip 3 and 7` → process each in turn.
- **Ambiguous draft id**: if the user says "send draft" with no number and there's exactly one pending → send it. If multiple → ask which.

## Reading the file safely

Use Node or PowerShell `Get-Content -Raw | ConvertFrom-Json` to read. Use `ConvertTo-Json -Depth 10` to write back. Or use the view/edit/create tool family directly. Always preserve the `_comment` field at the top.


